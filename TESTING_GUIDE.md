# Руководство по тестированию нового функционала

## 📋 Подготовка к тестированию

### 1. Установка зависимостей Frontend

```bash
cd frontend
npm install
```

Это установит новые зависимости для PDF экспорта:
- `jspdf` - генерация PDF
- `html2canvas` - конвертация HTML в изображение

### 2. Запуск миграции базы данных

```bash
cd backend
python migrate_add_suppliers_customers.py
```

Эта миграция создаст следующие таблицы:
- `suppliers` - поставщики
- `supplier_orders` - заказы поставщикам
- `supplier_order_items` - позиции заказов
- `supplier_contracts` - договоры с поставщиками
- `customers` - клиенты
- `customer_segments` - сегменты клиентов
- `customer_purchases` - покупки клиентов
- `customer_interactions` - взаимодействия с клиентами

### 3. Запуск серверов

**Backend:**
```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

**Frontend:**
```bash
cd frontend
npm run dev
```

## 🧪 Тестирование функционала

### Тест 1: Глобальный поиск

1. Откройте приложение в браузере
2. Нажмите `Ctrl+K` или кликните на кнопку поиска в шапке
3. Введите запрос (например, название поставщика или клиента)
4. Проверьте:
   - ✅ Поиск открывается по `Ctrl+K`
   - ✅ Результаты появляются при вводе минимум 2 символов
   - ✅ Можно навигироваться стрелками ↑↓
   - ✅ Можно выбрать результат Enter
   - ✅ Поиск работает по поставщикам, клиентам, товарам
   - ✅ `Escape` закрывает поиск

**Ожидаемый результат:** Поиск находит записи по всем модулям и позволяет быстро перейти к ним.

### Тест 2: Горячие клавиши

1. Нажмите `Ctrl+/` для открытия справки по горячим клавишам
2. Проверьте отображение всех доступных комбинаций
3. Протестируйте основные комбинации:
   - `Ctrl+K` - открыть поиск
   - `Ctrl+N` - создать запись (в любом модуле)
   - `Escape` - закрыть модальное окно

**Ожидаемый результат:** Справка открывается, все комбинации работают корректно.

### Тест 3: Управление поставщиками

1. Перейдите в раздел "Поставщики" в меню
2. Нажмите "Добавить поставщика" или `Ctrl+N`
3. Заполните форму:
   - Название (обязательно)
   - Контактное лицо
   - Телефон
   - Email
   - Реквизиты (ИНН, КПП, ОГРН)
   - Банковские реквизиты
   - Рейтинг
4. Сохраните поставщика
5. Проверьте:
   - ✅ Поставщик появился в списке
   - ✅ Можно редактировать
   - ✅ Можно удалить
   - ✅ Поиск работает
   - ✅ Пагинация работает

**Ожидаемый результат:** CRUD операции работают корректно, данные сохраняются в БД.

### Тест 4: Управление клиентами

1. Перейдите в раздел "Клиенты" в меню
2. Нажмите "Добавить клиента" или `Ctrl+N`
3. Заполните форму:
   - Имя (обязательно)
   - Тип (физическое/юридическое лицо)
   - Контакты
   - Для юр. лиц - реквизиты
   - Сегмент (если есть)
4. Сохраните клиента
5. Проверьте:
   - ✅ Клиент появился в списке
   - ✅ Можно редактировать
   - ✅ Можно удалить
   - ✅ Кнопка "Обновить метрики" работает
   - ✅ Отображаются метрики (покупки, LTV, средний чек)

**Ожидаемый результат:** CRUD операции работают, метрики рассчитываются корректно.

### Тест 5: PDF экспорт

1. Откройте любой отчет (например, ОДДС)
2. Найдите кнопку экспорта PDF (если добавлена)
3. Или используйте утилиту напрямую в консоли браузера:
   ```javascript
   import { exportToPDF } from './utils/pdfExport'
   const element = document.getElementById('report-content')
   exportToPDF(element, { title: 'Тестовый отчет' })
   ```
4. Проверьте:
   - ✅ PDF файл генерируется
   - ✅ Содержимое корректно отображается
   - ✅ Заголовки и форматирование сохранены

**Ожидаемый результат:** PDF файл создается с корректным содержимым.

### Тест 6: API endpoints

Проверьте работу API через Swagger UI: http://localhost:8000/docs

**Поставщики:**
- `GET /api/suppliers/` - список поставщиков
- `POST /api/suppliers/` - создать поставщика
- `GET /api/suppliers/{id}` - получить поставщика
- `PUT /api/suppliers/{id}` - обновить поставщика
- `DELETE /api/suppliers/{id}` - удалить поставщика
- `GET /api/suppliers/orders/` - список заказов
- `GET /api/suppliers/contracts/` - список договоров

**Клиенты:**
- `GET /api/customers/` - список клиентов
- `POST /api/customers/` - создать клиента
- `GET /api/customers/{id}` - получить клиента
- `PUT /api/customers/{id}` - обновить клиента
- `DELETE /api/customers/{id}` - удалить клиента
- `POST /api/customers/{id}/update-metrics` - обновить метрики
- `GET /api/customers/segments/` - список сегментов

**Ожидаемый результат:** Все endpoints работают, возвращают корректные данные.

## 🐛 Известные проблемы и ограничения

1. **PDF экспорт:**
   - Требует установки библиотек `jspdf` и `html2canvas`
   - Может работать медленно на больших отчетах
   - Графики могут отображаться некорректно (используется html2canvas)

2. **Глобальный поиск:**
   - Поиск работает только по активным записям
   - Результаты ограничены 5 записями на модуль
   - Не ищет по описаниям и дополнительным полям

3. **Метрики клиентов:**
   - Автоматически не обновляются при создании покупок
   - Требуется ручное обновление через кнопку "Обновить метрики"

## ✅ Чеклист тестирования

- [ ] Глобальный поиск открывается по Ctrl+K
- [ ] Поиск находит поставщиков
- [ ] Поиск находит клиентов
- [ ] Поиск находит товары
- [ ] Горячие клавиши работают (Ctrl+/, Ctrl+N, Escape)
- [ ] Создание поставщика работает
- [ ] Редактирование поставщика работает
- [ ] Удаление поставщика работает
- [ ] Создание клиента работает
- [ ] Редактирование клиента работает
- [ ] Удаление клиента работает
- [ ] Обновление метрик клиента работает
- [ ] PDF экспорт работает (если библиотеки установлены)
- [ ] API endpoints отвечают корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах backend

## 📝 Отчет о тестировании

После тестирования заполните:

**Дата тестирования:** ___________

**Тестировщик:** ___________

**Результаты:**
- Глобальный поиск: ✅ / ❌
- Горячие клавиши: ✅ / ❌
- Поставщики: ✅ / ❌
- Клиенты: ✅ / ❌
- PDF экспорт: ✅ / ❌

**Найденные ошибки:**
1. ___________
2. ___________

**Рекомендации:**
1. ___________
2. ___________

