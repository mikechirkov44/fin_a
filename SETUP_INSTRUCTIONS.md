# Инструкции по настройке

## ✅ Выполненные шаги

### 1. Миграция базы данных
Миграция для таблицы `dashboard_widgets` успешно выполнена.

### 2. Установка зависимостей Frontend
Зависимости `jspdf` и `html2canvas` успешно установлены.

### 3. Настройка переменных окружения

#### Для Frontend:
Создайте файл `.env` в папке `frontend/` со следующим содержимым:

```env
# WebSocket URL для уведомлений в реальном времени
VITE_WS_URL=ws://localhost:8000

# API URL (если нужно переопределить)
# VITE_API_URL=http://localhost:8000
```

**Примечание:** Если файл `.env` не создан, приложение будет использовать значения по умолчанию:
- WebSocket URL: `ws://localhost:8000`
- API URL: `http://localhost:8000` (настроен в vite.config.ts)

## 📋 Проверка работоспособности

### Проверка миграции:
```bash
# Подключитесь к базе данных и проверьте наличие таблицы
psql -U postgres -d fin_a -c "\d dashboard_widgets"
```

### Проверка зависимостей:
```bash
cd frontend
npm list jspdf html2canvas
```

### Запуск приложения:

**Backend:**
```bash
cd backend
uvicorn app.main:app --reload
```

**Frontend:**
```bash
cd frontend
npm run dev
```

## 🔧 Дополнительные настройки

### WebSocket соединение
- По умолчанию WebSocket подключается к `ws://localhost:8000/ws/notifications`
- При разрыве соединения происходит автоматическое переподключение (до 10 попыток)
- Ping/Pong механизм поддерживает соединение активным

### PDF экспорт
- Требует установленные библиотеки: `jspdf` и `html2canvas`
- Экспорт доступен в отчетах: ОДДС, ОПУ, Баланс
- Формат по умолчанию: A4, портретная ориентация

### Настраиваемые дашборды
- API доступен по адресу: `/api/dashboard-widgets/`
- Поддерживаются типы виджетов: metric, chart, table
- Виджеты привязаны к пользователю и компании

### Расширенная аналитика
- Доступна по адресу: `/analytics`
- Включает: прогнозирование, сравнение периодов, ABC/XYZ анализ
- Требует наличия данных в системе для корректной работы

