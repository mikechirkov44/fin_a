# Быстрый старт для тестирования

## 🚀 Шаги для запуска

### 1. Установка зависимостей Frontend
```bash
cd frontend
npm install
```

### 2. Запуск миграции БД
```bash
cd backend
python migrate_add_suppliers_customers.py
```

### 3. Запуск серверов

**Терминал 1 - Backend:**
```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

### 4. Откройте браузер
Перейдите на http://localhost:3000

## ✅ Быстрая проверка функционала

### Тест 1: Глобальный поиск (30 секунд)
1. Нажмите `Ctrl+K`
2. Введите любое слово (минимум 2 символа)
3. Проверьте, что появляются результаты
4. Нажмите `Escape` для закрытия

**Ожидаемый результат:** ✅ Поиск открывается и работает

### Тест 2: Горячие клавиши (10 секунд)
1. Нажмите `Ctrl+/`
2. Проверьте, что открывается окно со списком горячих клавиш

**Ожидаемый результат:** ✅ Окно открывается

### Тест 3: Поставщики (2 минуты)
1. Перейдите в меню "Поставщики"
2. Нажмите "Добавить поставщика" или `Ctrl+N`
3. Заполните:
   - Название: "Тестовый поставщик"
   - Телефон: "+7 999 123-45-67"
4. Нажмите "Сохранить"
5. Проверьте, что поставщик появился в списке

**Ожидаемый результат:** ✅ Поставщик создается и отображается

### Тест 4: Клиенты (2 минуты)
1. Перейдите в меню "Клиенты"
2. Нажмите "Добавить клиента" или `Ctrl+N`
3. Заполните:
   - Имя: "Тестовый клиент"
   - Телефон: "+7 999 765-43-21"
4. Нажмите "Сохранить"
5. Проверьте, что клиент появился в списке

**Ожидаемый результат:** ✅ Клиент создается и отображается

### Тест 5: API (1 минута)
1. Откройте http://localhost:8000/docs
2. Найдите раздел "suppliers"
3. Попробуйте выполнить `GET /api/suppliers/`
4. Проверьте ответ

**Ожидаемый результат:** ✅ API возвращает данные

## 🐛 Если что-то не работает

### Ошибка: "Module not found"
```bash
cd frontend
npm install
```

### Ошибка: "Table does not exist"
```bash
cd backend
python migrate_add_suppliers_customers.py
```

### Ошибка: "Cannot find module 'jspdf'"
```bash
cd frontend
npm install jspdf html2canvas
```

### Ошибка: "CORS"
Убедитесь, что backend запущен на порту 8000, а frontend на 3000

### Ошибка: "500 Internal Server Error"
Проверьте логи backend в терминале, где запущен uvicorn

## 📝 Чеклист быстрой проверки

- [ ] Frontend запускается без ошибок
- [ ] Backend запускается без ошибок
- [ ] Миграция БД выполнена успешно
- [ ] Глобальный поиск работает (`Ctrl+K`)
- [ ] Горячие клавиши работают (`Ctrl+/`)
- [ ] Можно создать поставщика
- [ ] Можно создать клиента
- [ ] API endpoints доступны в Swagger

## 🎯 Следующие шаги после успешного тестирования

1. Протестировать редактирование и удаление
2. Протестировать поиск в модулях
3. Протестировать пагинацию
4. Добавить PDF экспорт в отчеты
5. Продолжить с настраиваемыми дашбордами

